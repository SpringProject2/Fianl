<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="g">
	
	<!-- 갤러리 조회  -->
	<select id="gallery_list" resultType="gallery" parameterType="int">
		select * from gallery where gallIdx=#{idx} order by galleryContentRef DESC
		<!-- idx 내림차순 -->
	</select>

	<!-- select를 제외한 insert, update, delete태그는 resultType을 기입하지 않는다 -->
	<!-- 새 글 추가 -->
	<insert id="gallery_insert" parameterType="gallery">
		insert into GALLERY values(
			#{galleryContentRef},
			#{galleryContent},
			#{galleryFileName},
			SYSDATE,
			#{gallIdx},
			#{galleryFileExtension},
			#{galleryLikeNum}
        )
	</insert>
	
	<!-- idx에 해당하는 게시글 갯수 구하기 -->
	<select id="countNum" resultType="int" parameterType="int">
		select count(*) from gallery where gallIdx=#{idx}
	</select>
	
	<!-- idx에 해당하는 최신 게시글 가져오기 -->
	<select id="maxNum" resultType="int" parameterType="int">
		select max(galleryContentRef) from gallery where gallIdx=#{idx}
	</select>
	
	<!-- 글 삭제 -->
	<delete id="gallery_delete" parameterType="java.util.HashMap">
		delete from gallery where gallIdx=#{1} and galleryContentRef=#{2}
	</delete>
	
	<!-- 수정을 위한 게시글 한 건 조회 -->
	<select id="gallery_one" resultType="gallery" parameterType="java.util.HashMap">
		select * from gallery where gallIdx=#{1} and galleryContentRef=#{2}
	</select>
	
	<!-- 글 수정 -->
	<update id="gallery_update" parameterType="gallery">
		update gallery set
			galleryContent=#{galleryContent},
			galleryFileName=#{galleryFileName},
			galleryRegdate=sysdate,
			galleryFileExtension=#{galleryFileExtension},
			galleryLikeNum=#{galleryLikeNum}
			where gallIdx=#{gallIdx} and galleryContentRef=#{galleryContentRef}
	</update>
</mapper>












